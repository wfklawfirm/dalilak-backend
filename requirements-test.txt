# Test dependencies — install alongside requirements.txt
# pip install -r requirements.txt -r requirements-test.txt
#
# P0 security/CI audit (2026-07): this file previously pinned httpx==0.27.0
# while requirements.txt pins httpx==0.27.2. `pip install -r requirements.txt
# -r requirements-test.txt` (exactly what .github/workflows/backend-ci.yml
# runs) fails with ResolutionImpossible on that conflict — CI was broken.
# httpx is already provided by requirements.txt; no separate pin needed here.
pytest==8.3.2
